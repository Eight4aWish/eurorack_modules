// secrets.h.example — copy to `secrets.h` (same folder) and fill in.
// `secrets.h` is gitignored.
//
//     cp include/esp32-clklinkrec/secrets.h.example include/esp32-clklinkrec/secrets.h
//     # then edit secrets.h
//
// Used by the esp32-clklinkrec WiFi diagnostic and (eventually) the
// Ableton Link integration. SSID must be a dual-band network if you want
// to land on 5 GHz — most routers expose a single SSID that the C5 picks
// a band on automatically based on signal strength.

#pragma once

#define WIFI_SSID "your-network-name"
#define WIFI_PASS "your-network-password"

// Optional fallback for the Recorder discovery path. The firmware first
// tries mDNS (_recorder._tcp.local. via Bonjour). If that doesn't resolve
// within 2 s and this define is present, the firmware uses it as the
// recorder's address. Format is hostname[:port] or ip[:port]; port
// defaults to 8765. Leave commented out if mDNS works on your LAN.
// See docs/RECORDER_PROTOCOL.md for the full discovery contract.
//
// #define RECORDER_HOST "macbook.local"
